<!-- AI-generated -->

# Справочник по API (API Reference)

### Модуль `widget.py` (Пользовательские виджеты)

* #### `mask_account_card`
  ```python
  def mask_account_card(card_data: str) -> str:
  ```
  <!-- Логика: разделяет строку по пробелу с конца, чтобы изолировать цифровой номер от названия -->
  **Объяснение:** Принимает строку с названием и номером (например, `"Visa Gold 7000792289606361"` или `"Счет 73654108430135874305"`). Функция самостоятельно отделяет номер от названия платежной системы, определяет тип (карта или счет) и возвращает строку с корректно замаскированным номером, сохраняя исходное название.
  
  **Пример использования:**
  ```python
  from widget import mask_account_card

  print(mask_account_card("Visa Platinum 7000792289606361"))
  # Выведет: Visa Platinum 7000 79** **** 6361
  ```

* #### `get_date`
  ```python
  def get_date(date: str) -> str:
  ```
  <!-- Разрезает ISO-строку по символу дефиса и берет первые два символа от третьего блока (день) -->
  **Объяснение:** Принимает строку с датой в международном формате ISO (например, `"2024-01-12T11:04:13.203411"`). Нарезает строку на части по разделителям и собирает дату в привычном формате `"ДД.ММ.ГГГГ"`.
  
  **Пример использования:**
  ```python
  from widget import get_date

  print(get_date("2024-05-18T16:20:00.123456"))
  # Выведет: 18.05.2024
  ```

---

### Модуль `processing.py` (Обработка списков)

* #### `filter_by_state`
  ```python
  def filter_by_state(operations: list[dict[str, Any]], state: str = "EXECUTED") -> list[dict[str, Any]]:
  ```
  <!-- Ручная итерация по списку. Возвращает новый независимый массив словарей -->
  **Объяснение:** Фильтрует входной список банковских операций по их статусу. Вручную перебирает элементы цикла и собирает **новый список** словарей, у которых значение ключа `state` совпадает с переданным аргументом (по умолчанию ищет `"EXECUTED"`).
  
  **Пример использования:**
  ```python
  from processing import filter_by_state

  operations = [{"id": 1, "state": "EXECUTED"}, {"id": 2, "state": "CANCELED"}]
  print(filter_by_state(operations, state="CANCELED")) 
  # Выведет: [{'id': 2, 'state': 'CANCELED'}]
  ```

* #### `get_date` *(внутренний помощник)*
  ```python
  def get_date(op: dict[str, Any]) -> str:
  ```
  <!-- Безопасный метод .get() предотвращает падение в KeyError при отсутствии даты -->
  **Объяснение:** Вспомогательная функция, которая принимает словарь операции и безопасно извлекает значение ключа `"date"`. Используется в качестве ключа сортировки.

* #### `sort_by_date`
  ```python
  def sort_by_date(operations: list[dict[str, Any]], reverse: bool = True) -> list[dict[str, Any]]:
  ```
  <!-- Встроенная функция sorted не изменяет исходный список operations (иммутабельность) -->
  **Объяснение:** Принимает список банковских операций и сортирует их по дате. Создает новый отсортированный список с помощью функции `sorted()`, не изменяя оригинальный массив. По умолчанию сортировка идет от самых свежих к старым (`reverse=True`).

---

### Модуль `masks.py` (Низкоуровневое маскирование)

* #### `get_mask_card_number`
  ```python
  def get_mask_card_number(card_number: str) -> str:
  ```
  <!-- Цикл форматирует строку блоками по 4 знака. Индексы от 6 до 11 включительно заменяются на звездочки -->
  **Объяснение:** Принимает чистую 16-значную строку с номером карты. Посимвольно перебирает её в цикле, скрывает центральные цифры (с 6 по 11 индексы) символом `*` и каждые 4 символа добавляет пробел для удобного вывода в формате `XXXX XX** **** XXXX`.

* #### `get_mask_account`
  ```python
  def get_mask_account(card_number: str) -> str:
  ```
  <!-- Отрицательный срез [-4:] извлекает ровно четыре последних символа строки счёта -->
  **Объяснение:** Принимает строку с полным номером счета. Возвращает строку, состоящую из двух звездочек и четырех последних цифр счета с помощью среза `[-4:]` (формат `**XXXX`).

---

### Модуль `generators.py` (Эффективные генераторы)

* #### `filter_by_currency`
  ```py
  def filter_by_currency(transactions: list[dict[str, Any]], currency: str) -> Iterator[dict[str, Any]]:
  ```
  <!-- Круглые скобки запускают генераторное выражение (Lazy evaluation) для экономии памяти -->
  **Объяснение:** Принимает на вход список транзакций и трехбуквенный код валюты. Использует генераторное выражение `(...)` для фильтрации транзакций. Не хранит подходящие элементы в памяти, а возвращает экономичный **итератор**.

* #### `transaction_descriptions`
  ```py
  def transaction_descriptions(transactions: list[dict[str, Any]]) -> Generator[str, None, None]:
  ```
  <!-- Ключевое слово yield замораживает выполнение функции до вызова следующего элемента -->
  **Объяснение:** Функция-генератор. Проходит по списку транзакций и при каждой новой итерации через `yield` возвращает описание транзакции. Если ключ `"description"` отсутствует, возвращает дефолтное значение `"Описание отсутствует"`.

* #### `card_number_generator`
  ```python
  def card_number_generator(start: int, stop: int) -> Iterator[str]:
  ```
  <!-- Спецификатор :016_ автоматически генерирует строку нужной длины с разделителями разрядов -->
  **Объяснение:** Принимает начальное и конечное числа диапазона и запускает цикл. На каждом шаге преобразует текущее число в 16-разрядный формат с ведущими нулями, заменяет разделители на пробелы и отдает готовую строку через `yield`.